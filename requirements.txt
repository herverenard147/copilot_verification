# Interface web (front HTML/CSS/JS servi par FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9        # upload d'images (POST /api/extract)

# Comptes utilisateurs, base de donnees, corrections (RGPD)
SQLAlchemy>=2.0
argon2-cffi>=23.1              # hash de mot de passe (argon2id)

# Interface technique de secours (Streamlit, non maintenue mais conservee)
streamlit>=1.32

# Donut (extraction image -> JSON)
torch>=2.0
transformers>=4.38

# Donnees et calcul
pandas>=2.0
numpy>=1.24

# Visualisation (notebooks d'exploration)
seaborn>=0.13
matplotlib>=3.8

# Machine learning (baseline, evaluation)
scikit-learn>=1.3

# Recherche semantique (onglet Questions)
sentence-transformers>=2.5
faiss-cpu>=1.7

# Prompt engineering / LLM (backends interchangeables)
groq>=0.9
google-genai>=0.3

# Images + pretraitement (redressement, CLAHE)
Pillow>=10.0
opencv-python-headless>=4.9    # CLAHE + detection des bords (src/preprocess.py)
PyMuPDF>=1.24                  # rasterise la 1ere page d'une facture PDF avant extraction (pas de poppler system requis)

# Import d'ecritures/bilan externes (Excel/CSV/Word)
openpyxl>=3.1                  # lecture .xlsx (pandas.read_excel)
python-docx>=1.1               # lecture des tableaux dans un .docx

# Scaling horizontal (optionnel) : cache partage sessions/jobs/limites entre
# plusieurs instances. Sans REDIS_URL configuree, repli total sur la memoire
# locale du process -- aucune regression pour un usage mono-instance.
redis>=5.0

# Scaling horizontal (optionnel) : base comptes/corrections/bilan partagee
# entre plusieurs instances. Sans DATABASE_URL configuree, repli sur SQLite
# local (comportement historique, aucune regression en mono-instance).
psycopg[binary]>=3.1

# Tests
pytest>=8.0
